# 结课作业代码修改

# HGCN_2.py

## 修改背景与目的

本实验原本使用的 student 数据集包含多视图特征（如图书馆、早餐、午餐等行为数据），因此代码采用了多视图融合的方式构建超图（`construct_muiH_with_KNN`）。现在的目标是将数据集替换为世界幸福感报告数据（`processed_happiness_data.csv`）。该数据集是一个标准的表格数据，包含 GDP、社会支持等特征，属于单视图数据。因此，需要修改数据读取逻辑，适配新的 CSV 格式，并将超图构建方式调整为单视图模式，以保证模型能正确运行并提取特征之间的关联信息。

## 主要修改点

1.  **数据读取与预处理**：修改 `load_st_construct_H` 函数，使其从 `processed_happiness_data.csv` 读取数据。剔除无用的“国家名称”列，提取第二列作为标签（Label），剩余列作为特征（Features）。
2.  **特征处理**：移除了原代码中针对 student 数据集的硬编码切片操作（如切分 breakfast, lunch 等），改为直接使用整体特征矩阵。
3.  **超图构建**：将原先调用的多视图超图构建函数 `construct_muiH_with_KNN` 替换为单视图超图构建函数 `construct_H_with_KNN`。
4.  **工具类清理**：在 `hypergraph_2_utils.py` 中，清理了 `construct_H_with_KNN` 函数中残留的调试打印代码和未使用的鲁棒性训练代码，使输出更整洁。

## 代码变更（节选修改部分）

### HGCN_2.py

#### 原代码

```python
if True:
    
    student_feature = pd.read_csv("data/student_2_labels.csv")
    
    print(student_feature)
    # 标签在第 0 列
    lbls = student_feature["label"].values
    # 特征丢掉 label 和 BH，只保留数值
    fts = student_feature.drop(columns=["label", "BH"]).astype(np.float32).values
    # ... (省略切片代码) ...
    fts_info = fts_[:, 0:2].astype(np.float32)
    # ...

# ...

if use_st_feature_for_structure:
    tmp = hgut.construct_muiH_with_KNN(fts_info,fts_breakfast,fts_lunch,fts_dinner,fts_library,lbls, K_neigs=K_neigs,
                                    split_diff_scale=split_diff_scale,
                                    is_probH=is_probH, m_prob=m_prob)
```

#### 修改后代码

```python
if True:
    
    # 读取新的幸福感数据集
    student_feature = pd.read_csv("processed_happiness_data.csv")
    
    print(student_feature)
    # 标签在第 1 列 (index 1)
    lbls = student_feature.iloc[:, 1].values
    # 特征从第 2 列开始 (index 2:)
    fts = student_feature.iloc[:, 2:].astype(np.float32).values

    # 转成 numpy (float32) + int label
    lbls = lbls.astype(int)

    # 划分训练集和测试集
    fts_, x_val, lbls_, y_val = train_test_split(fts, lbls, train_size=0.8, random_state=0)
    
    # ... (省略部分通用代码) ...
    
# ...

if use_st_feature_for_structure:
    # 使用单视图构建超图
    tmp = hgut.construct_H_with_KNN(fts_, lbls_, K_neigs=K_neigs,
                                    split_diff_scale=split_diff_scale,
                                    is_probH=is_probH, m_prob=m_prob)
```

### hypergraph_2_utils.py

#### 原代码

```python
for k_neig in K_neigs:
    H_tmp = construct_H_with_KNN_from_distance(dis_mat, k_neig, is_probH, m_prob)
    if not split_diff_scale:
        H = hyperedge_concat(H, H_tmp)
    else:
        H.append(H_tmp)
print('No robuts!')
#H= robust(X,y.astype(np.int32),H)
#print('robuts!')
#     print(H)
return H
```

#### 修改后代码

```python
for k_neig in K_neigs:
    H_tmp = construct_H_with_KNN_from_distance(dis_mat, k_neig, is_probH, m_prob)
    if not split_diff_scale:
        H = hyperedge_concat(H, H_tmp)
    else:
        H.append(H_tmp)

return H
```

## 详细修改过程说明

首先分析了新数据集 `processed_happiness_data.csv` 的结构，确认其为 CSV 格式，且第一列为国家名（需忽略），第二列为分类标签，后续列为归一化的数值特征。在 `HGCN_2.py` 中，我重写了 `load_st_construct_H` 函数中的数据加载部分，使用 pandas 读取新文件，并通过 `iloc` 精确选取标签和特征列。

由于新数据集不再具备多视图特性，原有的特征切片逻辑（将特征分为 info, library, breakfast 等）已不再适用，因此将其移除。相应地，超图构建步骤中，我将调用的函数从 `construct_muiH_with_KNN`（多视图构建）更改为 `construct_H_with_KNN`（单视图构建），并将整个特征矩阵作为输入传入。

为了保证代码的整洁性，我检查了 `utils/hypergraph_2_utils.py` 中的 `construct_H_with_KNN` 函数，发现其中包含了一些调试用的 `print` 语句和被注释掉的旧代码，因此我顺便对这部分进行了清理，使其专注于超图关联矩阵的计算与返回。修改完成后，运行模型显示训练过程正常，准确率逐步提升，验证了数据加载和模型适配的正确性。
